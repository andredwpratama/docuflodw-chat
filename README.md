# docuflodw-chat
A simple chatbot that answers questions based on a document pasted by the user. No document retrieval is used—the entire document is loaded into the context window. The main focus is on learning to design a robust prompt system and properly manage the context window.
